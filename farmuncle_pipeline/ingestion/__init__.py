"""The runnable Phase C ingestion scripts: live_tick, daily_rewrite,
historical_backfill, retry_failed_pages. Run as modules from the repo
root, e.g.:

    python -m farmuncle_pipeline.ingestion.live_tick
"""
